auth:
  jwt:
    secret: ${JWT_SECRET:}
    algorithm: HS256
    expiration: ${JWT_EXPIRATION:86400}
    private_key: ${JWT_PRIVATE_KEY:}
    public_key: ${JWT_PUBLIC_KEY:}
  rbac:
    default_role: user
    roles:
      - admin
      - manager
      - user
      - viewer
